#ifndef DATACACHE_H
#define DATACACHE_H

#include <QObject>
#include <QDebug>

class DataCache : public QObject
{
    Q_OBJECT
public:
    explicit DataCache(QObject *parent = nullptr);

signals:

public slots:

private slots:

private:    
};

#endif // DATACACHE_H
